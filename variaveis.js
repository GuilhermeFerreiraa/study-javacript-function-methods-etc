/*

variaveis 

    var
    let
    const

    var clima = "Quente"
    clima = "frio"
    console.log(clima)

    let clima = "Quente"
    clima = "frio"
    console.log(clima)

    a VAR e LET, são muito semelhantes, porém a Const é contínua, não altera
 
    */
    
    //A variável CONSTANTE não pode ser alterada em qualquer momento 
    const clima = "Quente"
    console.log(clima)

